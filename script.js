function datascroll(){
    const targetdiv = document.getElementById('info');
    targetdiv.scrollIntoView({behaviour:'smooth'});
}

function achievements() {
    const targetdiv = document.getElementById('achievements');
    targetdiv.scrollIntoView({behaviour:'smooth'});
}

function tools() {
    const targetdiv = document.getElementById('tools');
    targetdiv.scrollIntoView({behaviour:'smooth'});
}

function projects() {
    const targetdiv = document.getElementById('projects');
    targetdiv.scrollIntoView({behaviour:'smooth'});
}

function contact() {
    const targetdiv = document.getElementById('contact');
    targetdiv.scrollIntoView({behaviour:'smooth'});
}
